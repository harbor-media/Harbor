import type { JSX } from "react";

// Placeholder until Task 9 fills in the featured hero and catalog rows. The
// navigation and sign-out that used to live here now belong to the app shell.
export function Home(): JSX.Element {
  return (
    <main className="mx-auto max-w-[1600px] px-6 py-8">
      <h1 className="font-display text-2xl">Home</h1>
    </main>
  );
}
