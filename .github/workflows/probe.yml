name: Probe

# Temporary diagnostic. Uses no third-party actions, so if this runs while CI
# fails at startup, the blocker is the org's Actions policy on external actions.
on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "runner reached; no external actions used"
      - run: node --version
